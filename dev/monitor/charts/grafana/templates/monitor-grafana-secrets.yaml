apiVersion: v1
kind: Secret
metadata:
  name: deis-monitor-grafana-creds
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    app: deis-monitor-grafana
type: Opaque
data:
  db_password: {{ .Values.db_password | b64enc | quote }}
  user_password: {{ .Values.password | b64enc | quote }}
