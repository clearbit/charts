apiVersion: v1
kind: Secret
metadata:
  name: {{ template "fullname" . }}-aws
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  credentials: |-
    {{ .Values.awsCreds | b64enc }}
